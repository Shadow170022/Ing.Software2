<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".formulario_cambiar"></activity>
        <activity android:name=".formulario_recuperar" />
        <activity android:name=".enviar_codigo_2" />
        <activity android:name=".Enviar_Codigo_1" />
        <activity android:name=".Loading_Screen" />
        <activity android:name=".Formulario_Login" />
        <activity android:name=".Formulario_Sign_In" />
        <activity android:name=".Login_Sign" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>